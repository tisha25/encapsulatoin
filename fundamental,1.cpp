#include <iostream>
using namespace std;
bool isunmeric(string str){
	for(int i=0;i<str.length();i++)
	if (isdisgit(str[i]==false);
	return false /when one non numerice value is found; return false
	return true;
	}
	int main(){
		string str;
		cout<<"enter a string :"
		cin>>str;
		if(isnumeric(str))
		cout<<"this is number"<<endl;
		
	}
